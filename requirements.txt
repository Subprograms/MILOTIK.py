# requirements.txt – Python libraries

pandas>=2.0
numpy>=1.24
scikit-learn>=1.4          # provides sklearn.tree, preprocessing, metrics …
imbalanced-learn>=0.12     # BalancedRandomForestClassifier
regipy>=2.0                # RegistryHive parsing
joblib>=1.3
chardet>=5.2
graphviz>=0.20             # Python bindings for DOT → PNG rendering

# How?
# python -m pip install --upgrade pip
# python -m pip install -r requirements.txt
